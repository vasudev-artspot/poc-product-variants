query categories {
  categoriesSearch(searchText: "") {
    ... on CategoryQueryError {
      code
      message
    }
    ... on CategoryQuerySuccess {
      __typename
      code
      categories {
        name
        id
      }
    }
  }
}
